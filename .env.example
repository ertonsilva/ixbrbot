# ============================================================================
# IX.br Status Bot - Configuration
# ============================================================================

# Telegram Bot Token (REQUIRED)
# Get your token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# RSS Feed URL
# Default: IX.br status page RSS feed
RSS_FEED_URL=https://status.ix.br/rss

# Check interval in seconds (minimum: 60)
# How often to check the RSS feed for new events
CHECK_INTERVAL=300

# Maximum age for messages in days (1-30)
# Events older than this won't be sent
MAX_MESSAGE_AGE_DAYS=7

# Database path (inside container)
# SQLite database for storing subscriptions and sent messages
DATABASE_PATH=/app/data/ixbr_bot.db

# Health check file path (inside container)
# Used by Docker health check to verify bot is running
HEALTH_CHECK_FILE=/app/data/health

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Rate limiting: maximum commands per minute per chat
RATE_LIMIT_COMMANDS=10

# Quiet hours (optional, global default)
# Format: HH:MM (24-hour)
# Users can override with /silencio command
# QUIET_HOURS_START=22:00
# QUIET_HOURS_END=07:00

# ============================================================================
# Admin Configuration
# ============================================================================

# Admin user IDs (comma-separated)
# These users can use /backup, /restore, /stats commands
# Get your user ID by messaging @userinfobot on Telegram
ADMIN_USER_IDS=

# Auto backup settings
# Enable automatic daily backups (sent at 3 AM)
BACKUP_ENABLED=false

# Chat ID to send automatic backups to
# Usually your private chat with the bot or a private group
# Get chat ID by adding the bot to the chat and using /status
BACKUP_CHAT_ID=

# ============================================================================
# Security Configuration
# ============================================================================

# Maximum backup file size in bytes (default: 1MB = 1048576)
# Security measure to prevent upload of large malicious files
MAX_BACKUP_SIZE=1048576
